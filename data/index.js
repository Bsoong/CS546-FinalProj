const courseData = require("./courses");
const userData = require("./users");
const ratingData = require("./ratings")

module.exports = {
  courses: courseData,
  users: userData,
  ratins: ratingData
};
